1、只需要label.png图面，命名为：pano_heading_label.png
2、一定要去除水印
3、可以按照heading，同时开12个python进程加速。
4、一定要有log，有cache，能从断点开始继续分割。
5、分割完毕后一定要double check，修改data_cleaner以适应带有分割图片的数据。
6、分割文件存放于Label_Pictures文件夹中，还是以城市名作为目录。
7、先尝试一个3线程同时推进的算法。


流程：
读取三个filter_name.txt
利用这个test.py，将城市名字作为参数之一，用一个perform_segmentation.py开始分割，用命令行调用test.py
3个线程同时推进。



test部分：

1、先做一个48图小test看看水印的影响，看看需不需要去除水印，或者干脆直接去除。
2、rgb和label图都要，看看效果。
3、



真正的分割：

1、数据放在/data/yyh中，ENet放在/home/yyh中，项目带着数据走，在data的Citywander目录下进行代码修改工作。
2、封装成类，使用init()初始化，指定各种模式参数，使用:
将分割封装成：

init():
city_name=
city_wander_dir=
input_dir=
output_di=
rgb_or_label=
rgb_img_dir=
label_img_dir=

配置模式，参数

读取city_name_filered文件：
semantic_segmentation(img_name,input_dir,output_dir,rgb_or_label)

3、需要有一个分割log和cache存储当前分割进度。
4、分割后要进行校验操作，确保所有街景已经分割。